node_modules/
dist/
.env
.env.local
*.log
coverage/
.DS_Store

# One-off account backups taken before a destructive fix. These hold a bcrypt hash and
# encrypted PII — keep them local, never commit them.
dead-admin-*.json
